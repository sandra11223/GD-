# MongoDB Atlas Connection String
# Replace <username>, <password>, and <cluster-url> with your actual MongoDB Atlas credentials
# Example: mongodb+srv://myuser:mypassword@cluster0.xxxxx.mongodb.net/global-education-council?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/global-education-council?retryWrites=true&w=majority

# JWT Secret Key - Change this to a secure random string in production
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Server Port
PORT=5000

# Environment
NODE_ENV=development
